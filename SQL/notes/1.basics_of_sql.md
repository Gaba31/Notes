## What is SQL ?
### SQL is a programming language that is used to manage and manipulate relational databases.SQL deals with structured data,that is data which is organized in tables following a strict format.

We can do these using SQL (Structed Query Language):  
1. Create Table
2. Modify Table
3. Retrieve Table
4. Delete Table 
5. Modify Access


### Main Categories of SQL Commands :
1. DDL (Data Definition Language)  
2. DML (Data Manipulation Language)  
3. DCL (Data Control Language)  



### Data Definition
SQL provides commands to define the structure of a
database, collectively known as Data Definition Language
(DDL). Some common DDL commands include:

* CREATE:Used to create databases, tables, indexes, and more.
* ALTER:Used to modify existing database objects (e.g., add or remove a column from a table).
* DROP:Used to delete entire databases, tables, or other objects.


### Manipulating Data
Data Manipulation Language (DML) commands let you insert,
update, or delete data within the database tables. These
commands include:

* SELECT:Retrieves data from one or more tables.
* INSERT:Adds new records into a table.
* UPDATE:Modifies existing records in a table.
* DELETE:Removes records from a table.

### Data Control Language (DCL)
DCL commands are used to control access to data in a
database.
* GRANT:Gives a user access privileges to database objects.
* REVOKE:Removes access privileges from a user.


### Key Features and Advantages of Using SQL
1. Simplicity: SQL uses an English-like syntax (e.g., SELECT, UPDATE,
WHERE), making it relatively easy to learn.
2. Ubiquity: Nearly every relational database system supports SQL. Once
you learn core SQL, you can apply it to MySQL, PostgreSQL, Oracle, and
more.
3. Flexibility: You can handle complex queries involving multiple joined
tables, subqueries, window functions, and more.
When used properly with indexes and query optimization,
4. Performance:
SQL can query large volumes of data very efficiently.
5. Data Integrity. Features like transactions, constraints (PRIMARY KEY,
FOREIGN KEY, UNIQUE), and ACID compliance ensure that your data
remains accurate and consistent.

## Installation :
At Installing mysql it ask TCP/IP to check the checkbox . What does that mean ?  
Ans -> TCP stands for Transmission control protocol and it sends data in form of packets in set of order . Whereas IP which stands for Internet Protocol is responsible for the location that we are going to send the data .

### History of MySql :
Started in 1195 then in 2008 owned by sun microsystem and again in 2010 sun microsystem owned by oracle. It is a open source software .